# Safe Area / Status Bar QA — 2025-11-07

## Playwright simulation runs
Command: `cd webapp && npx playwright test tests/qa/safe-area.spec.ts -c playwright.qa.config.ts`

| Device (simulated) | Mode | Evidence |
| --- | --- | --- |
| iPhone 15 Pro Max (tos override) | Fullscreen | `safe-area.spec.ts` test "status-bar shell exposes fullscreen attribute" validates `data-fullscreen="true"` + `/debug_safe_area` snapshot. |
| Pixel 8 Pro (android override) | Default sheet | Test "mobile platforms hide manual close button" ensures `manual-close-button` отсутствует и `data-fullscreen="false"`. |
| Telegram Desktop | Default sheet | Test "desktop platforms render manual close button" проверяет видимость кнопки и вызов `Telegram.WebApp.close`. |

> Примечание: проверки выполнялись в Playwright с кастомными safe-area/viewport параметрами через `setupStageMocks`. Скриншоты/логи можно воспроизвести из отчёта `webapp/playwright-report-qa/` и консольных логов.
