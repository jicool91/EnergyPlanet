# Safe Area / Status Bar QA — 2025-11-07

## Playwright simulation runs
Command: `cd webapp && npx playwright test tests/qa/safe-area.spec.ts -c playwright.qa.config.ts`

| Device (simulated) | Mode | Evidence |
| --- | --- | --- |
| iPhone 15 Pro Max (iOS override) | Fullscreen | `safe-area.spec.ts` test "debug command dumps safe-area snapshot (iPhone fullscreen)" валидирует `/debug_safe_area` и `data-fullscreen="true"`. |
| iPhone 15 Pro Max (iOS override) | Default sheet | Test "iPhone portrait default sheet keeps fullscreen attr false" подтверждает `data-fullscreen="false"`. |
| Pixel 8 Pro (Android override) | Fullscreen | Test "Pixel fullscreen exposes attribute" проверяет `data-fullscreen="true"`. |
| Pixel 8 Pro (Android override) | Default sheet | Test "mobile platforms hide manual close button" гарантирует отсутствие `manual-close-button` и sheet режим. |
| Telegram Desktop | Default sheet | Test "desktop platforms render manual close button" проверяет кнопку и вызов `Telegram.WebApp.close`. |

> Примечание: проверки выполнялись в Playwright с кастомными safe-area/viewport параметрами через `setupStageMocks`. Скриншоты/логи можно воспроизвести из отчёта `webapp/playwright-report-qa/` и консольных логов.
