# 📋 Глубокий UI/UX-аудит Energy Planet (Telegram Mini App)

Дата: 5 ноября 2025 года  
Методология: эвристический анализ + проверка цветовой системы + аудит производительности интерфейса. Основано на адаптированном промпте UX Cabin и собственных сценариях продукта.citeturn2search3

## 1. Контекст и ключевые сценарии
- **Цель продукта:** быстрый тап-цикл с прогрессией экономики, бустами, постройками и социальными механиками.
- **Основные флоу:** TapScreen (фарм энергии), Exchange (магазин Stars + постройки), Earn (квесты), Prestige-контур, социальные панели (друзья, лидерборд).
- **Точки входа:** старт из Telegram Mini App 2.0 в полноэкранном режиме, push-возвраты, deeplink из промо-ботов.

## 2. Сильные стороны, которые важно сохранить
- **Глубокая интеграция с Telegram-темой:** `webapp/src/styles/tokens.css` + `webapp/src/utils/telegramTheme.ts:1` корректно подтягивают большинство themeParams и safe-area значений.
- **Компонентная модульность:** крупные части UI вынесены в самостоятельные панели (`TapScreen`, `ShopPanel`, `BuildingsPanel`, `BoostHub`) и переиспользуют карточную сетку.
- **Обратная связь:** хаптика (`webapp/src/hooks/useHaptic.ts`) и тосты (`useNotification`) подключены к ключевым событиям (покупки, апгрейды).

## 3. Основные проблемы по слоям

### 3.1 Структура экранов и навигация
| Серьёзность | Описание | Доказательство | Рекомендация |
| --- | --- | --- | --- |
| Major | Вкладка “Постройки” не заполняется каталогом при пустом `/buildings`; пользователи видят заглушку даже при наличии прогресса. | `BuildingsPanel.tsx` зависит от `buildingCatalog`, не формируя запасных карточек из состояния игры. | Добавить объединение каталога с `gameStore.buildings` (исполнено в текущем MR). |
| Major | `Virtuoso` получает контейнер без фиксированной высоты → список не рендерится. | `BuildingsPanel.tsx:256` (`className="flex-1"`) без родительской высоты. | Ввести `style={{ height: '100%', minHeight: 320 }}` и обёртку с `flex-1`. |
| Minor | `TabPageSurface` не учитывает растущие панели → в некоторых состояниях UI обрезается по нижнему краю. | `TabPageSurface.tsx:17` — завязано на flex-1 без границ. | Проверить safe-area после фуллскрина (вошло в рекомендации). |

### 3.2 Цветовая система и визуальный слой
- **Наблюдение:** текущая тёмная палитра держится на золото-серых оттенках, но ряд компонентов (карточки, фильтры) используют прямые RGBA-градиенты, не связанные с токенами (`BuildingCard.tsx:109` до правки).
- **Риски:** не выполняются тренды 2025 (мягкие градиенты, AI-эстетика) и рекомендации Telegram Mini Apps 2.0 о кастомизации лоадеров и фуллскрина.citeturn7news0
- **Практики 2025:** яркие неоновые/градиентные акценты + 3D-цвета применяются точечно, но требуют контраста ≥4.5:1.citeturn9search5turn8search0
- **Текущее состояние:** 
  - `webapp/src/styles/tokens.css` использует токены, но fallback'и для светлой темы ограничены.
  - `BuildingCard` и кнопки покупок применяют кастомные значения («glass» градиент), не синхронизированные с токенами.

### 3.3 Доступность и фуллскрин
- Telegram Mini Apps 2.0 требуют поддержки полноэкранных режимов, ориентации, гироскопа, добавления на 홈-экран.citeturn7news0
- `index.css:37-103` фиксирует `html` в `position: fixed` и отключает скролл — в фуллскрине может конфликтовать с обновлёнными viewport-флагами и жестами.
- Проверка контраста выявила строки с ~3.4:1 (secondary текст на золотистом фоне). Требуется привязка ко всем токенам и автоматическое тестирование.citeturn8search0

### 3.4 Перфоманс и анимации
- `TapCircle` анимирует glow без лимита fps. При массовых эффектов и Mini Apps 2.0 (gyro, fullscreen) нужно адаптивное отключение.citeturn7news0
- Глобальные частицы не привязаны к `preferencesStore.reduceMotion`.

- [x] **Fallback для построек:** `buildingsViewModel.ts` объединяет каталог с реальными данными и добавляет синтетические карточки, чтобы игрок видел прогресс даже при сбоях API.
- [x] **Фикс `Virtuoso`:** контейнер получил `minHeight` и высоту по `--tg-viewport-height`, поэтому лента построек стабильно открывается даже в full-screen.citeturn7news0
- [x] **Обновление палитры:** `tokens.css` и `tailwind.config.js` получили вторую акцентную линейку (`accent-cyan`/`magenta`), градиенты `--gradient-ai/soft` и светлую тему с AA-контрастом для золотых CTA.
- [x] **Компоненты без жёстких RGBA:** `Button`, `Card`, `BuildingCard`, `BuildingsPanel` и Tap-статусы используют токены для фонов/рамок/теней, focus-стили унифицированы через `ring-accent-gold`.
- [x] **Reduce motion на практике:** `TapCircle`, `TapParticles`, `ModalBase`, `ProgressBar`, `LevelUpScreen` и capabilities-хук учитывают `preferencesStore.reduceMotion` + системный `prefers-reduced-motion`.citeturn10search2
- [x] **Контраст в CI:** добавлен `npm run test:contrast`, который парсит токены и проверяет пары текста/фона на ≥4.5 : 1 (включая светлую тему).citeturn8search0
- [x] **Viewport + gyro:** сервис `tma/viewport` слушает как SDK, так и нативный `viewportChanged`/`safeAreaChanged`, обновляет алиасы `--layout-viewport-*`, добавлены методы `requestFullscreen/exitFullscreen/expandViewport`. Появился сервис `tma/motion` и хук `useGyroscope`, TapCircle получает параллакс от гироскопа.citeturn1search0turn1search1turn6view0
- [x] **A/B + визуал-тесты:** реализован `experimentsStore` с экспериментом `palette_v1` (dual-accent vs classic), CTA на TapScreen реагируют на вариант, в Playwright настроен `npm run test:visual` (превью `visual.html`) со скриншотами офлайн-модалки.

## 5. Приоритетные действия (roadmap)
| Спринт | Что сделать | Сутки | Метрика |
| --- | --- | --- | --- |
| 45 (✅) | Расширили токены, Tailwind и контрастный чек для светлой темы. | 2 | `npm run test:contrast` без ошибок; CTA ≥4.5 : 1.citeturn8search0 |
| 46 (✅) | Переписаны viewport-хуки, добавлены нативные события `viewportChanged`, fallback fullscreen и сервис гироскопа. | 3 | 0 визуальных дефектов в полноэкранном режиме; удержание с фуллскрина не падает.citeturn7news0 |
| 47 (✅ частично) | Интегрировали `reduceMotion`: отключили glow/particles и адаптировали прогресс-бары. | 2 | ≤1 accessibility issue; 100% тестов `prefers-reduced-motion`.citeturn10search2 |
| 48 | Внедрить цветовой регламент 2025: палитра мягких/неон градиентов с контрастом, синхронизировать с токенами. | 3 | Отчёт PaletteLint без ошибок; визуальная валидация с трендами.citeturn9search5turn9search6 |

## 6. Цветовая палитра — что изменить
- **Текущие проблемы:** 
  - `--color-accent-gold` используется как универсальный акцент → монотонность, конфликт с dark-mode при низком контрасте.
  - Карточки используют насыщенные тени, которые создают «грязный» фон.
- **Рекомендации:**
  1. Создать палитру «AI-neon» (циан/фуксия) и «soft gradients» для второстепенных блоков, вдохновляясь тенденциями 2025.citeturn9search5turn9search6
  2. Определить пресеты для разных статусов (ROI, предупреждения, бусты) с проверенным контрастом ≥4.5:1 (AA).citeturn8search0
  3. Расширить токены светлой темы, чтобы минимизировать ручные hex-коды.

## 7. Метрики и валидация
- **Продуктовые:** retention вкладки “Постройки” (переходы → покупки), конверсия в апгрейд.
- **UX:** доля пользователей, видящих список построек, bounce rate в Exchange.
- **Технические:** средний FPS при включенных анимациях, количество accessibility-ошибок (axe).
- **Цвет/тема:** автоматические snapshot-тесты (Chromatic/Storybook) + `npm run test:contrast` в CI; визуальные проверки `npm run test:visual` для ключевых модалок.

## 8. План A/B-эксперимента по новой палитре
- **Гипотеза:** двухакцентная палитра (золото + неон-циан/магент) увеличит конверсию в покупку/апгрейд без потери читабельности, подсвечивая лучшие ROI-карточки и бусты.citeturn11search0
- **Дизайн:** делим трафик 50/50 на вкладке «Постройки»: контроль — текущая золотая схема, тест — карточки ROI/бусты используют `accent-cyan`/`accent-magenta` + `gradient-ai`. На время испытания отключаем арбитражные промо, чтобы не смешивать эффекты.
- **Метрики:** основная — CTR по «Купить»/«Апгрейд»; дополнительные — время до первой покупки, удержание вкладки, жалобы на читабельность (бот фидбек).citeturn10search4
- **Продолжительность:** минимум 7 дней или ≥5 000 уникальных игроков для детекта Δ=+5 % к CTR при α=0.05.
- **Guardrails:** мониторим результаты `npm run test:contrast`, ручной прогон axe, FPS-телеметрию и churn игроков, чтобы остановить эксперимент при регрессе UX.
- **Артефакты:** сохраняем отчёт в `docs/design/UI_UX_ANALYSIS.md` и выгружаем дашборд в Superset по итогам.

## 9. Открытые вопросы
- Нужен ли отдельный light-профиль для пользователей с системной светлой темой?
- Как согласовать новые палитры с бренд-ассетами (иконки, маркетинг)?
- Есть ли бэкэнд-/экономические зависимости для дополнительных статусов построек?

---

**Следующие шаги:**
1. Завершить рефакторинг токенов и обновить документацию (`docs/design/DESIGN_SYSTEM_GUIDE.md`).
2. Подготовить визуальные тесты и репорты по контрасту.
3. Согласовать roadmap с продуктом и дизайном (check-in на спринт 46). 
