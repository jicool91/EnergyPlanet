# 🎨 Цветовая система — текущее состояние и план апдейта

Обновлено: 2 ноября 2025 года  
Основано на анализе `webapp/src/styles/tokens.css`, компонентных классов и трендах 2025.citeturn9search5turn9search6

## 1. Текущее состояние
- **Базовая палитра:** `--color-accent-gold` и его оттенки (`_light`, `_dark`) выступают универсальным акцентом.
- **Поверхности:** тёмные фоны (`--color-bg-secondary`, `--color-bg-tertiary`) без адаптации под светлую тему (fallback есть, но не индивидуален).
- **Градиенты:** кастомные RGBA (например, в `BuildingCard.tsx` до правки) не завязаны на токены → сложно обеспечить контраст.
- **Контраст:** вторичный текст (`--color-text-secondary`) на золотистых фонах выдаёт ~3.4:1, что ниже требования WCAG 2.2.citeturn8search0

## 2. Сравнение с трендами 2025
| Тренд | Что рекомендуют | Что у нас | Разрыв |
| --- | --- | --- | --- |
| **AI-neon & holographic gradients** | Использовать яркие неон/микро-градиенты аккуратно, сохраняя читаемость.citeturn9search5 | Золото + серые, единый градиент в CTA. | Нужен второй акцент (циан/фуксия) для выделения ROI/бустов. |
| **Soft, layered surfaces** | Мягкие прозрачности, стёкла, но с контролем контраста.citeturn9search6 | Жёсткие тени и насыщенные фоны. | Перейти на полупрозрачные плоскости с токенами. |
| **Доступность по умолчанию** | Контраст ≥4.5:1, адаптивная тема.citeturn8search0 | Вторичные тексты иногда падают до 3.4:1. | Ввести «контрастные» варианты токенов и автотесты. |

## 3. План апдейта палитры
1. **Расширить токены:**
   - `--color-accent-cyan`, `--color-accent-magenta`, `--gradient-ai`, `--gradient-soft`.
   - Для светлой темы вычислить отдельные значения, избегая «перелома» контраста.
2. **Обновить компоненты:**
   - CTA → solid fill (золото) + hover-градиент.
   - Карточки → `--color-surface-secondary` по умолчанию, ключевые карточки (ROI) → `--gradient-ai`.
   - Фильтры (`BuildingsPanel`, `ShopPanel`) → токены вместо вручную заданных RGBA.
3. **Валидировать цвета:**
   - Storybook screenshot + axe проверки.
   - Порог контраста ≥4.5:1 (текст) и ≥3.0:1 (иконки/графика).citeturn8search0

## 4. Чек-лист перед внедрением
- [ ] Добавлены новые токены в `tokens.css` и `tailwind.config.js`.
- [ ] Светлая тема получила индивидуальные значения (не fallback из тёмной).
- [ ] Компоненты `Button`, `Card`, `BuildingCard`, `ShopPanel` используют токены.
- [ ] Chromatic/Storybook вариант прошёл визуальное сравнение.
- [ ] Документация обновлена: `docs/design/DESIGN_SYSTEM_GUIDE.md`, `docs/ui_audit/02_audit_report.md`.

## 5. Следующие шаги
- Подготовить эксперимент с новыми градиентами на вкладке «Постройки» (A/B тест — второй акцент против классического золота).
- Собрать обратную связь от пользователей по читабельности (короткий опрос внутри сообщества).
- По итогам — обновить брендовые материалы (иконки, баннеры) под новую палитру.
